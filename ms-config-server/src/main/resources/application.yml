server:
  port: 6060

spring:
  application:
    name: ms-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/markmercado1/DeliveryProyect.git
          searchPaths:
            - config-data
          default-label: main

  security:
    user:
      name: root
      password: 123456
